<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:PolyhydraGames.Core.Maui.Controls;assembly=PolyhydraGames.Core.Maui"
             xmlns:views="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"
             x:Class="PFAssistant.Maui.Controls.ExpandableView "
             x:Name="View">
    <Frame BackgroundColor="{DynamicResource defaultBackgroundColor}">
        <StackLayout>
            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                <StackLayout.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding Expand}" />
                </StackLayout.GestureRecognizers>
                <Label x:Name="TitleLabel"  HorizontalOptions="Start" />
                <Label x:Name="SymbolLabel" TextColor="Black" />
                <controls:LabelButton BackgroundColor="Transparent" HorizontalOptions="EndAndExpand" Text="Add"
                                      TextColor="Black" Command="{Binding MenuCommand}" />
            </StackLayout>

            <BoxView HeightRequest="2" Color="White" />
            <views:Expander IsExpanded="{Binding IsExpanded}" x:Name="expand"/>
        </StackLayout>
    </Frame>
</ContentView>